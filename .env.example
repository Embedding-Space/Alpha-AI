# Alpha AI Configuration

# Server settings
HOST=0.0.0.0
PORT=8000

# Default model (e.g., ollama:qwen2.5:14b, openai:gpt-4o-mini, anthropic:claude-3-sonnet)
DEFAULT_MODEL=ollama:qwen2.5:14b

# Database
DATABASE_URL=sqlite:///./alpha_ai.db

# MCP servers configuration (optional)
# Path to a Claude Desktop format JSON file defining MCP servers
# MCP_CONFIG_FILE=/path/to/mcp_config.json

# List of MCP servers to enable (comma-separated)
# If not specified, all servers in the config file will be enabled
# MCP_SERVERS=alpha-brain,context7,fetch

# API Keys (add as needed)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GROQ_API_KEY=

# Ollama configuration
OLLAMA_BASE_URL=http://localhost:11434/v1